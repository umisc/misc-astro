-- Initial D1 migration for misc-astro
--
-- Intentionally a no-op for now: no tables are created yet.
-- Add real tables here when ready. Example:
-- CREATE TABLE IF NOT EXISTS example (
--   id INTEGER PRIMARY KEY AUTOINCREMENT,
--   created_at TEXT NOT NULL DEFAULT (datetime('now')),
--   name TEXT NOT NULL
-- );

SELECT 1;
