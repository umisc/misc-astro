---
import { cn } from '@/lib/utils';

type Props = {
  class?: string;
};

const { class: className } = Astro.props as Props;
---

<div class={cn('mx-auto w-full max-w-7xl px-page-inline', className)}>
  <slot />
</div>
